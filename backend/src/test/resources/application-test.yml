spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:plamingo;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false
  data:
    redis:
      repositories:
        enabled: false
  docker:
    compose:
      enabled: false

management:
  health:
    redis:
      enabled: false

app:
  auth:
    jwt:
      secret: test-only-secret-key-that-is-at-least-32-bytes
  integrations:
    google-maps:
      api-key: test
    openai:
      api-key: ""
      base-url: http://localhost
      model: gpt-5-nano
    aws:
      access-key-id: test
      secret-access-key: test
      s3-bucket: test
